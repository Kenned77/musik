<%= form_with(model: song_artist, local: true) do |form| %>
  <% if song_artist.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(song_artist.errors.count, "error") %> prohibited this song-artist relationship from being saved:</h2>

      <ul>
      <% song_artist.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :song_id %>
    <%= form.collection_select :song_id, Song.order(:title), :id, :title, include_blank: true %>
  </div>

  <div class="field">
    <%= form.label :artist_id %>
    <%= form.collection_select :artist_id, Artist.order(:name), :id, :name, include_blank: true %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
